{
  "name": "pong",
  "version": "1.0.0",
  "private": true,
  "description": "Single-player Pong on an HTML 2D canvas, small enough to type from memory.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
